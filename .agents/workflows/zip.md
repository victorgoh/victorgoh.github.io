---
description: Create a manual_setup.zip file in the root of the repository.
---

Adopt the **Verse Scripter** persona from `.agents/agents.md`.

Use the **zip** skill to create a zip file in the root of this repository, naming it as `manual_setup.zip`, to zip the `.agents/`, `preferences/`, `.claude/`, and `.grok/` folders, and the `AGENTS.md` and `CLAUDE.md` files.
